"use strict"; // treat all code as newer versions of JS

// [mostly it is automatically treated as newer version.]

//alert(2 + 3); // this syntax will run in the browser, we are using node

let name = "harshil"; // string
let age = 20; // number => 2 to power 53
let isLoggedIn = false; // boolean [true, false]
let temperature = null; // null [standalone value]
let rollNo; // undefined [not assigned]

// bigint for big numbers
// symbol => unique
// object

console.log(typeof age);

console.log(typeof null); // null is object
